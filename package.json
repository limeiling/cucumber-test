{
  "dependencies": {
    "@cucumber/cucumber": "^10.6.0",
    "playwright": "^1.44.0"
  },
  "devDependencies": {
    "chai": "^5.1.1",
    "cucumber-html-reporter": "^7.1.1"
  },
  "scripts": {
    "test": "echo Running tests...",
    "generate-report": "npx cucumber-js features/ -f json:reports/cucumber_re.json --exit",
    "test-with-report": "npm test && npm run generate-report && node index.js"
  }
}
