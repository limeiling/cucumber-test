module.exports = {
  // Specify the directory where feature files are located
  features: './features',

  // Specify the directory where step definitions are located
  steps: './step-definitions',

  // Specify the format and output path for the JSON report
  format: ['json:reports/results.json'],
};
